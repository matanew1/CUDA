# Made by: Matan Bardugo
# ID: 207489774

# Instructions:

1.  Edit the existing `mf` file. You should write like this:
    <ip address of the current run machine>
    <ip address of the second machine>
2. Write in command line: `make build` or `make` for create `*.o` files for execute.
3. Write in command line: `make run` on one machine and `make runOn2` for two machines.
3. Write in command line: `make clean` for clear the *.o files

# You should see the sum in the output is equals to the SIZE constant if myProto.h file.
